# FlareSolverr 服务地址（可选，默认 http://localhost:8191/v1）
# 启动 FlareSolverr：docker run -d --name flaresolverr -p 8191:8191 ghcr.io/flaresolverr/flaresolverr:latest
FLARESOLVERR_URL=http://localhost:8191/v1

# 输出基目录（html/ 和 content/ 统一存放于此目录下）
BASE_OUTPUT_DIR=outputs
